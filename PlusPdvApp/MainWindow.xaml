<Window x:Class="PlusPdvApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PlusPdvApp"
        mc:Ignorable="d"
        Title="PlusPdv App" Height="650" Width="850"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryColor" Color="#FF007ACC"/>
        <SolidColorBrush x:Key="SecondaryColor" Color="#FFE0E0E0"/>
        <SolidColorBrush x:Key="TextColor" Color="#FF333333"/>
        <SolidColorBrush x:Key="ErrorColor" Color="#FFD32F2F"/>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="5,2"/>
        </Style>

        <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="22"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,10,0,15"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryColor}"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderBrush" Value="{StaticResource SecondaryColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderBrush" Value="{StaticResource SecondaryColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="10 20"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderBrush" Value="{StaticResource SecondaryColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource SecondaryColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource SecondaryColor}"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#FFF9F9F9"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="RowHeight" Value="30"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="CanUserResizeRows" Value="False"/>
            <Setter Property="CanUserReorderColumns" Value="False"/>
            <Setter Property="CanUserSortColumns" Value="True"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" x:Name="LoginPanel" Background="White"
                HorizontalAlignment="Center" VerticalAlignment="Center"
                Width="350" Padding="25" 
                CornerRadius="20,20,20,20">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="5" Color="LightGray" Direction="270" BlurRadius="10" Opacity="0.5"/>
            </Border.Effect>

            <StackPanel>
                <TextBlock Style="{StaticResource SectionTitleStyle}" Margin="0,0,0,20">
                    <Run Text="PlusPdvApp"/>
                </TextBlock>

                <TextBlock Text="Loja"/>
                <TextBox x:Name="StoreTextBox" Text="{Binding StoreId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="Auto" HorizontalAlignment="Stretch" Height="36" FontSize="14" />

                <TextBlock Text="Usuário:"/>
                <TextBox x:Name="UsernameTextBox" Text="{Binding Username, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="Auto" HorizontalAlignment="Stretch" Height="35" />

                <TextBlock Text="Senha:" Margin="5,10,5,2"/>
                <TextBox x:Name="PasswordTextBox" Text="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="Auto" HorizontalAlignment="Stretch" Height="36" />

                <TextBlock x:Name="LoginStatusTextBlock" Text="{Binding LoginStatusMessage}" TextWrapping="Wrap" Margin="5"
                           HorizontalAlignment="Center" TextAlignment="Center"
                           Foreground="{StaticResource ErrorColor}"/>

                <Button Content="Entrar" Width="286" Height="30" Padding="4,2" FontSize="12"
                        Margin="2"
                        Command="{Binding LoginCommand}"
                        IsEnabled="{Binding IsLoginEnabled}"
                        VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="0" x:Name="ProductSearchGrid" Visibility="{Binding ProductSearchVisibility}" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Buscar Produto" Style="{StaticResource SectionTitleStyle}"/>

            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="4" HorizontalAlignment="Center">
                <TextBlock Text="Nome do Produto:" VerticalAlignment="Center"/>
                <TextBox x:Name="ProductNameTextBox" Width="200" Text="{Binding ProductName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Text="Ordenar por:" VerticalAlignment="Center" Margin="20,0,5,0"/>
                <ComboBox x:Name="SortOrderComboBox" Width="120" 
                          ItemsSource="{Binding SortOrderOptions}"
                          SelectedItem="{Binding SelectedSortOrder}">
                </ComboBox>

                <Button Content="Buscar Produto" Width="130" Margin="20,0,0,0" Command="{Binding SearchProductsCommand}"/>
            </StackPanel>

            <TextBlock Grid.Row="2" x:Name="ProductStatusTextBlock" Text="{Binding ProductStatusMessage}" TextWrapping="Wrap" Margin="5"
                       HorizontalAlignment="Center" TextAlignment="Center"
                       Foreground="{StaticResource ErrorColor}"/>

            <DataGrid Grid.Row="3" x:Name="ProductDataGrid" IsReadOnly="True" Margin="5"
                      ItemsSource="{Binding Products}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="Auto"/>
                    <DataGridTextColumn Header="Nome" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="Fabricante" Binding="{Binding Manufacturer}" Width="*"/>
                    <DataGridTextColumn Header="Fornecedor" Binding="{Binding Supplier}" Width="*"/>
                    <DataGridTextColumn Header="Seção" Binding="{Binding Section}" Width="*"/>
                    <DataGridTextColumn Header="Subseção" Binding="{Binding Subsection}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</Window>